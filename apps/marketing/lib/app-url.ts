// URL of the real SourceTool dashboard (apps/web). The marketing site funnels
// sign-in / sign-up here rather than hosting its own dashboard.
export const APP_URL = process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000';
