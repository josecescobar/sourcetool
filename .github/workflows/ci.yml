name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    env:
      # prisma generate only needs this to be a well-formed value at schema-load
      # time — it never connects to it during typecheck/build.
      DATABASE_URL: postgresql://sourcetool:sourcetool@localhost:5432/sourcetool_test?schema=public
      DIRECT_DATABASE_URL: postgresql://sourcetool:sourcetool@localhost:5432/sourcetool_test?schema=public
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @sourcetool/db run db:generate
      - run: pnpm turbo typecheck
      - run: pnpm turbo lint

  test:
    name: Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: sourcetool
          POSTGRES_PASSWORD: sourcetool
          POSTGRES_DB: sourcetool_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://sourcetool:sourcetool@localhost:5432/sourcetool_test?schema=public
      DIRECT_DATABASE_URL: postgresql://sourcetool:sourcetool@localhost:5432/sourcetool_test?schema=public
      JWT_SECRET: test-secret
      JWT_REFRESH_SECRET: test-refresh-secret
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @sourcetool/db run db:generate
      - run: pnpm turbo build --filter=@sourcetool/shared --filter=@sourcetool/db
      - run: cd packages/db && npx prisma db push
      - run: pnpm turbo test

  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      # prisma generate only needs this to be a well-formed value at schema-load
      # time — it never connects to it during typecheck/build.
      DATABASE_URL: postgresql://sourcetool:sourcetool@localhost:5432/sourcetool_test?schema=public
      DIRECT_DATABASE_URL: postgresql://sourcetool:sourcetool@localhost:5432/sourcetool_test?schema=public
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @sourcetool/db run db:generate
      - run: pnpm turbo build
