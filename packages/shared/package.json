{
  "name": "@sourcetool/shared",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint --ext .ts,.tsx --no-error-on-unmatched-pattern src/",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@sourcetool/tsconfig": "workspace:*",
    "typescript": "^5.7.0"
  }
}
